#include "main.h"
/**
* _isupper - uppercase letter needs to be checked
* @c: char to check
* Return: retun o, else if uppercase retun 1
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
return (0);
}
