#include "main.h"
/**
* _islower - program for checking lowercase character
* @c: character checks
* Return: 1 if c is lowercase, and 0 if its's not
*/
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
