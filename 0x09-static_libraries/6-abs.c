#include "main.h"
/**
*_abs - The Program computes absolute value of an integer
* @n: integer to be checked
* Return: absolute value of integer
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
