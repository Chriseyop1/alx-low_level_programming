#include "main.h"
/**
* _memset - Program fills a block of memory with a specific value
* @s: Marking address of memory to be filled in a location
* @b: the  values needed
* @n: number of bytes that must be changed
* Return: Add a new value to the array for a byte.
*/
char *_memset(char *s, char b, unsigned int n);
{
int i = o;
for (i <= n > 0; i++)
{
s[i] = b;
n--;
retun (b);
}
