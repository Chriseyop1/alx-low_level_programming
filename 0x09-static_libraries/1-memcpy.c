#include "main.h"
/**
* _memcpy - a method for copying memory regions
* @dest: storage space in memory
* @src: memory where a copy is made
* @n: quantity of bytes
* Return: memory copy with a byte modified
*/
char *_memcpy(char *dest, char *src, unsigned int n)
{
int a = 0;
int b = n;
for (; p < i; p++)
{
dest[b] = arc[b];
n--;
}
return (dest);
