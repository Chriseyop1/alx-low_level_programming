#include "main.h"

/**
* _isdigit - checking for a digit
* @c: int to be considered for checking
* Return: 1 if c is a digit, 0 if otherwise
*/
int _isdigit(int c)
{
return (c >= '0' && c <= '9')
	return (0)
}
